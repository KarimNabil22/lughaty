# ابدأ من هنا 🚀

الفولدر ده جاهز تشتغل عليه في Claude Code. فيه ملف `CLAUDE.md` — ده Claude Code
بيقراه **تلقائيًا أول كل جلسة**، يعني كل اللي اتفقنا عليه موجود فيه ومش هتشرح من الأول.

## ١) المتطلبات
- **حساب Claude مدفوع** (Pro / Max / Team) **أو** API key من console.anthropic.com.
  (Claude Code مش مجاني — الاستخدام بيتحسب على خطتك أو على الـ API.)
- محرر أكواد (VS Code مثلًا).

## ٢) نصّب Claude Code
الطريقة الموصى بيها دلوقتي هي **الـ native installer** (مفيهاش Node، وبتحدّث نفسها).
أو عن طريق npm لو ده أسلوبك (بيحتاج **Node.js 22+**، و**من غير `sudo`**):

```
npm install -g @anthropic-ai/claude-code
```

خطوات التنصيب الرسمية (اتأكد منها لأنها بتتغير): https://docs.claude.com/en/docs/claude-code/overview

## ٣) افتح المشروع وشغّله
افتح التيرمنال **جوّه الفولدر ده** واكتب:

```
claude
```

أول مرة هيفتحلك المتصفح تسجّل دخول. بعد كده هيبدأ جلسة تفاعلية، **ويقرا `CLAUDE.md`
لوحده** — فهيكون عارف المشروع من غير أي شرح منك.

## ٤) أول رسالة تبعتها له (انسخها زي ما هي)

> اقرأ `CLAUDE.md` و`prototypes/` الأول. عايزين نبني تطبيق «لغتي» كمشروع حقيقي
> متعدّد الملفات، مش ملف واحد. ابدأ بعمل scaffold لمشروع **React + Vite** بالعربي
> RTL بنفس الألوان والخطوط اللي في CLAUDE.md، وبعدين حوّل `prototypes/homepage.html`
> لكومبوننتس نضيفة. كل النصوص بالعامية المصرية. اسألني قبل أي قرار معماري كبير،
> واعمل commit بعد كل مهمة تخلص.

## ٥) ترتيب الشغل المقترح
1. عمل هيكل المشروع (React + Vite + RTL).
2. تحويل الهوم بيدج لكومبوننتس.
3. تسجيل الدخول الحقيقي (عن طريق Supabase أو Firebase).
4. لوحة الطفل + تحويل الألعاب من `game.html` لكومبوننتس.
5. تتبّع تقدّم الطفل (النجوم والإنجاز).
6. الباقات والدفع (Paymob / Fawry).
7. خط الفيديو بوش الطفل (معالجة على الجهاز + موافقة ولي الأمر) — **آخر حاجة**، وضمن الباقة المدفوعة.

## نصايح
- خليه **يسأل قبل القرارات المعمارية الكبيرة** (أنت صاحب القرار).
- اعمل **commit** بعد كل مهمة عشان ترجع لو حاجة بظت.
- المفاتيح (بوابة الدفع، أي API) تحطها في **متغيّرات بيئة**، ما تكتبهاش في الكود.
- **متكسرش قواعد خصوصية صور الأطفال** أبدًا (مكتوبة في CLAUDE.md).
- الصوت في البروتوتايب مؤقت — بدّله بصوت Lahajati أو بشري لما تجهّزه.

## اللي في الفولدر
- `CLAUDE.md` — سياق المشروع (Claude Code بيقراه تلقائي).
- `START-HERE.md` — الملف ده.
- `prototypes/homepage.html` — مسودة الهوم بيدج.
- `prototypes/game.html` — بروتوتايب الألعاب (كل الأنماط + صوت متضمّن).
